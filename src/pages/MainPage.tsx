import * as React from 'react';
import RecipeList from '../components/RecipeList';

const MainPage = () => {
    return (
        <>
            <RecipeList/>
        </>
    )
};

export default MainPage;